# ScenePartner
# ScenePartner
# ScenePartner

## Running Project

If you don't have `uv` installed, run the one-time setup script:
```sh
# Mac/Linux
curl -LsSf https://astral.sh/uv/install.sh | sh

# Windows
powershell -ExecutionPolicy ByPass -c "irm https://astral.sh/uv/install.ps1 | iex"
```

```sh
uv run src/main.py
```